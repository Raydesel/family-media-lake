# Portfolio screenshots

Add PNG or WebP files here using the filenames referenced in the root `README.md`.
GitHub renders them inline when you push.

Recommended width: **1200–1400 px** (readable on desktop, not huge in the repo).
