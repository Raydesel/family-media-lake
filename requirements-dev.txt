# Local development + test dependencies (not deployed to Lambda).
boto3>=1.34
pytest>=8.0

# Needed to unit-test the enrichment Lambda locally (pip resolves versions
# compatible with your local Python; Lambda gets its own arm64/py311 wheels).
Pillow
pyarrow
# Only for local Python 3.8: old pyarrow needs pytz to read tz-aware
# timestamps (3.9+ uses the stdlib zoneinfo; Lambda runs 3.11).
pytz

